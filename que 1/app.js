 //que. is good string is a string which start with a and have more than 3 characters 
 // write program to find //

 let str ="apple";

if ((str[0] === 'a' )&& (str.length > 3)){
  console.log("good string");
} else {
  console.log("not a good string");
}